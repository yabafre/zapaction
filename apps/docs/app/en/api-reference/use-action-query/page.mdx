# useActionQuery

Use only for read paths.

```tsx
const query = useActionQuery(listUsers, {
  input: {},
  queryKey: usersKeys.list(),
  readPolicy: "read-only",
});
```

## Contract

- Requires explicit `queryKey`
- Requires `readPolicy: "read-only"`
- Infers input/output types from action contract
