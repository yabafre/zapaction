# Cache serveur vs client

| Couche | Role principal |
| --- | --- |
| Cache serveur Next | revalidation route/donnees |
| Cache React | memoization des fonctions serveur |
| Cache React Query | cycle de vie des donnees client |

Utilisez les tags pour garder la revalidation serveur et l'invalidation client alignees.
