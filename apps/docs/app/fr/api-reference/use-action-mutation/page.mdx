# useActionMutation

A utiliser pour les chemins d'ecriture.

```tsx
const mutation = useActionMutation(createUser);
await mutation.mutateAsync({ name: "Alex" });
```

## Invalidation

- Defaut: utilise `action.tags`
- Override possible via `invalidateWithTags`
- Desactivation via `invalidateOnSuccess: false`
