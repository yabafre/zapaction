# Client Data Fetching

`useActionQuery` est reserve aux lectures.

```tsx
const query = useActionQuery(getUsers, {
  input: {},
  queryKey: usersKeys.list(),
  readPolicy: "read-only",
});
```

Ne pas l'utiliser pour des ecritures.
