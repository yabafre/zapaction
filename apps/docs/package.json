{
  "name": "@zapaction/docs",
  "private": true,
  "description": "ZapAction documentation site (Mintlify)",
  "scripts": {
    "dev": "mint dev --port 3000 --no-open",
    "build": "mint broken-links",
    "start": "mint dev --port 3000 --no-open",
    "lint": "mint broken-links",
    "typecheck": "echo 'Mintlify docs: no TypeScript typecheck required'"
  },
  "devDependencies": {
    "mint": "^4.2.348"
  }
}
